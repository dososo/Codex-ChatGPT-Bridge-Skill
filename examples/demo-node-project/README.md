# Demo Node Project

Minimal dependency-free Node.js project for Codex ChatGPT Bridge examples.

Run:

```bash
node --test
```

This project is safe sample context for Packet / Manual fallback demos. It does not contain secrets, `.env`, private keys, cookies, or external network calls.
